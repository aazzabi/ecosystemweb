{% extends './Default/dashboard.html.twig' %}
{% block title %}
    Ajout un nouveau point de collecte
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/css/bootstrap-tagsinput.css') }}">
    <style>
        .card-block {
            padding: 10px;
        }

        .card-title {
            margin-bottom: 0px;
        }
    </style>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('assets/js/bootstrap-tagsinput.js') }} "></script>
    <script src="{{ asset('assets/js/typeahead.js') }} "></script>
    <script src="{{ asset('assets/js/competences.js') }} "></script>
{% endblock %}

{% block body %}
    <br>
        {{ form_start(form) }}
        <div class="row">
            <div class="col-md-6">
            </div>
            <div class="col-md-6 text-right">
                <br>
                <button type="submit" class="btn btn-success" value="Create">
                    <i class="fa fa-check" aria-hidden="true"></i>
                    Confirmer
                </button>
                <a href="{{ path('da_recyclage_index') }}" class="btn btn-secondary">
                    <i class="fa fa-undo" aria-hidden="true"></i>
                    Retour à la liste</a>

                <br>
            </div>
        </div>


    <hr>
    <div class="row">
        <div class="col-md-12">
            {% form_theme form 'bootstrap_3_layout.html.twig' %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
