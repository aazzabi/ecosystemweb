{% extends './Default/dashboard.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="row">
        <div class="col-md-6">
            <h1><b>Ajouter une mission </b></h1>
        </div>
        <div class="col-md-6 text-right">

            <br>
            <button type="submit" class="btn btn-success" value="Create">
                <i class="fa fa-check" aria-hidden="true"></i> Confirmer
            </button>

            <a href="{{ path('du_evenements_index') }}" class="btn btn-secondary">
                <i class="fa fa-undo" aria-hidden="true"></i>  Retour à la liste des missions</a>
            <br>
        </div>
    </div>
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
