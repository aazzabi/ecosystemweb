{% extends './Default/dashboard.html.twig' %}

{% block title %}
    Liste des sociétés
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/css/style.css') }}">
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function () {
            var table = $('#groupes').DataTable({
                "paging": true,
                "scrollX": true,
                "stateSave": true,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.13/i18n/French.json",
                }
            });

            table.columns().every(function (index) {
                var visible = this.visible();
                $('.toggle-vis').eq(index).prop('checked', visible);
            });

            $('.toggle-vis').on('change', function (e) {
                e.preventDefault();
                // Get the column API object
                var column = table.column($(this).attr('data-column'));
                // Toggle the visibility
                column.visible(!column.visible());
            });

        });

    </script>

{% endblock %}

{% block body %}

    {#Profiles liste et buttons#}
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-6">
                    <h1><b>Listes des sociétés</b></h1>
                </div>
                <div class="col-md-6 text-right">
                    <br>
                    <a href="{{ path('group_new') }}" class="btn btn-success">
                        <i class="icon-plus">
                            <i class="fa fa-plus" aria-hidden="true"></i>
                            &nbsp;Ajouter une société
                        </i>
                    </a>
                </div>
            </div>
        </div>
    </div>
    <hr>
    <div class="clearfix">
    </div>

<div class="row">
    <div class="col-md-12">
        <div class="card card-borderless">
            <ul class="nav nav-tabs nav-tabs-simple hidden-sm-down" role="tablist"
                 data-init-reponsive-tabs="dropdownfx">
                <li class="nav-item">
                    <a class="active" data-toggle="tab" role="tab" data-target="#societe" href="#"
                       aria-expanded="true">societe</a>
                </li>
                <li class="nav-item">
                    <a href="#" data-toggle="tab" role="tab" data-target="#association" class=""
                       aria-expanded="false">association</a>
                </li>
            </ul>
            <div class="nav-tab-dropdown cs-wrapper full-width hidden-md-up"><div class="cs-select cs-skin-slide full-width" tabindex="0"><span class="cs-placeholder">Hello World</span><div class="cs-options"><ul><li data-option="" data-value="#tab2hellowWorld"><span>Hello World</span></li><li data-option="" data-value="#tab2FollowUs"><span>Hello Two</span></li><li data-option="" data-value="#tab2Inspire"><span>Hello Three</span></li></ul></div><select class="cs-select cs-skin-slide full-width" data-init-plugin="cs-select"><option value="#tab2hellowWorld" selected="">Hello World</option><option value="#tab2FollowUs">Hello Two</option><option value="#tab2Inspire">Hello Three</option></select><div class="cs-backdrop"></div></div></div>
            <div class="tab-content">
                <div class="tab-pane active" id="societe" aria-expanded="true">
                    <table id="profiles" class="table table-striped table-bordered" cellspacing="0" width="100%">
                        <thead>
                        <tr>
                            <th>LOGO</th>
                            <th> Nom</th>
                            <th>Type</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for societe in societes %}
                            <tr>
                                <td>
                                    <a href="{{ path('group_show', { 'id': societe.id }) }}">
                                        <div id="profile-image1">
                                            {#{% if group.logo != '' %}#}
                                            {#<img alt="User Pic" src="{{ asset('uploads/logo/' ~ group.logo) }}"#}
                                            {#id="profile-image1" class="img-logo-societe img-responsive">#}
                                            {#{% else %}#}
                                            <img alt="User Pic" src="{{ asset('assets/img/NA.png') }}"
                                                 id="profile-image1" class="img-logo-societe img-responsive">
                                            {#{% endif %}#}

                                        </div>
                                    </a>
                                </td>
                                <td>{{ societe.name }}</td>
                                <td>{{ societe.type }}</td>
                                <td>
                                    <div class="btn-group btn-group-sm btn-group-horizontal">
                                        <a class="btn btn-secondary" href="{{ path('group_show', { 'id': societe.id }) }}"
                                           data-toggle="tooltip" title="Afficher le profile">
                                            <i class="fa fa-eye"></i>
                                        </a>
                                        <a class="btn btn-secondary" href="{{ path('group_edit', { 'id': societe.id }) }}"
                                           data-toggle="tooltip" title="Editer le profile">
                                            <i class="fa fa-pencil-square-o"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="tab-pane" id="association" aria-expanded="false">
                    <div class="row">
                        <div class="col-lg-12">

                            <table id="profiles" class="table table-striped table-bordered" cellspacing="0" width="100%">
                                <thead>
                                <tr>
                                    <th>LOGO</th>
                                    <th> Nom</th>
                                    <th>Type</th>
                                    <th>Actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for association in associations %}
                                    <tr>
                                        <td>
                                            <a href="{{ path('group_show', { 'id': association.id }) }}">
                                                <div id="profile-image1">
                                                    {#{% if group.logo != '' %}#}
                                                    {#<img alt="User Pic" src="{{ asset('uploads/logo/' ~ association.logo) }}"#}
                                                    {#id="profile-image1" class="img-logo-societe img-responsive">#}
                                                    {#{% else %}#}
                                                    <img alt="User Pic" src="{{ asset('assets/img/NA.png') }}"
                                                         id="profile-image1" class="img-logo-societe img-responsive">
                                                    {#{% endif %}#}

                                                </div>
                                            </a>
                                        </td>
                                        <td>{{ association.name }}</td>
                                        <td>{{ association.type }}</td>
                                        <td>
                                            <div class="btn-group btn-group-sm btn-group-horizontal">
                                                <a class="btn btn-secondary" href="{{ path('group_show', { 'id': association.id }) }}"
                                                   data-toggle="tooltip" title="Afficher le profile">
                                                    <i class="fa fa-eye"></i>
                                                </a>
                                                <a class="btn btn-secondary" href="{{ path('group_edit', { 'id': association.id }) }}"
                                                   data-toggle="tooltip" title="Editer le profile">
                                                    <i class="fa fa-pencil-square-o"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
