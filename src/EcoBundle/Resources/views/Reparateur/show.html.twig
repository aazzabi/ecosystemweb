{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reparateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Adresse</th>
                <td>{{ reparateur.adresse }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ reparateur.specialite }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ reparateur.horaire }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ reparateur.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('reparateur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('reparateur_edit', { 'id': reparateur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
