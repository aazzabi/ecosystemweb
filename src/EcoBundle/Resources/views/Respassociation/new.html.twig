{% extends 'base.html.twig' %}

{% block body %}
    <h1>Respassociation creation</h1>
    <div class="row">
        <div class="col-md-6">

        </div>
        <div class="col-md-6 text-right">
            <br>
            <button type="submit" class="btn btn-success" value="Create">
                <i class="fa fa-check" aria-hidden="true"></i> Confirmer
            </button>
            <a href="{{ path('respassociation_index') }}" class="btn btn-secondary">
                <i class="fa fa-undo" aria-hidden="true"></i>  Retour à la liste</a>
            <br>
        </div>
    </div>
    {{ form_start(form) }}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('respassociation_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
